# Hnefatafl Backend — Haskell Web API

Servant web API with Effectful effect system, linking against the C engine via FFI.

## Build & Test

```
hpack                  # regenerate hnefatafl.cabal from package.yaml (required after editing package.yaml)
cabal build            # build library + CLI
cabal test             # run test suite
cabal run cli          # run the CLI application
cabal clean            # full clean (required after C library changes)
```

After rebuilding `libhnefatafl.a`: `cabal clean && cabal build` (stale object files will cause link errors).

## Project Layout

```
package.yaml           Main config (hpack format) — edit this, NOT hnefatafl.cabal
hnefatafl.cabal        Auto-generated by hpack — never edit directly
cabal.project          Cabal project settings
fourmolu.yaml          Formatter config
db/schema.sql          SQLite schema
src/Hnefatafl/
  Core/Data.hs         Domain types: Game, Player, Move, ExternBoard, Layer, GameStatus, PlayerColor
  Bindings.hs          FFI to libhnefatafl: Storable types, DomainMapping, foreign imports
  Board.hs             Board utilities
  Serialization.hs     Move list serialization
  Api/
    Routes.hs          Top-level Servant API type
    Routes/Hotseat.hs  Hotseat game routes
    Routes/Online.hs   Online game routes (REST + WebSocket)
    Handlers.hs        Route handler implementations
    Handlers/Hotseat.hs
    Handlers/Online.hs
    Types.hs           API request/response types
    Types/Online.hs    Online-specific API types
  App/
    Hotseat.hs         Hotseat game application logic
    Online.hs          Online game application logic
    Storage.hs         Storage initialization
  Game/
    Common.hs          Shared game logic, PendingAction
    Hotseat.hs         Hotseat game logic
    Online.hs          Online multiplayer logic
    AI.hs              AI game logic (engine search integration)
  Effect/
    Clock.hs           Time effect
    IdGen.hs           ID generation effect
    Search.hs          Engine search effect
    Storage.hs         Storage effect (command algebra + transaction monad)
    WebSocket.hs       WebSocket effect
  Interpreter/
    Clock/IO.hs        Real clock
    IdGen/UUIDv7.hs    UUIDv7 generation
    Search/Local.hs    Local C engine search
    Search/Remote.hs   Remote engine search (HTTP)
    Search/Test.hs     Test stub
    Storage/SQLite.hs  SQLite transaction runner
    Storage/SQLite/    Per-entity query modules (Game, Move, Player, Token, PendingAction)
    WebSocket/IO.hs    Real WebSocket
  SelfPlay.hs          Engine self-play framework
  SelfPlay/Runner.hs   Self-play runner
  SelfPlay/UI.hs       Brick TUI for self-play
  Server.hs            Warp server setup
  CLI.hs               CLI option parsing
  Client.hs            Servant client for engine comparison
  Servant/WebSocket.hs Custom Servant WebSocket combinator
  QQ/Move.hs           QuasiQuoter for move literals
cli/Main.hs            CLI executable entry point
test/                  Tasty test suite (tasty-discover)
```

## Architecture

### Effect System (Effectful)

Effects are defined as GADTs in `Effect/`, interpreted in `Interpreter/`:

| Effect | Purpose | Interpreters |
|---|---|---|
| `Storage` | DB operations (command algebra + transaction monad) | `SQLite` |
| `Search` | Engine search | `Local` (C FFI), `Remote` (HTTP), `Test` (stub) |
| `Clock` | Current time | `IO` |
| `IdGen` | Generate unique IDs | `UUIDv7` |
| `WebSocket` | WebSocket communication | `IO` |

The Storage effect uses a two-layer pattern: `StorageCmd` (individual operations) composed into `StorageTx` (transactions), run via `runTransaction`.

### FFI Bindings (`Bindings.hs`)

The C library is linked statically via GHC linker flags in `package.yaml`. Key pattern:

1. `Storable*` types mirror C structs (derived via `derive-storable-plugin`)
2. `DomainMapping` typeclass converts between Storable ↔ domain types
3. `foreign import ccall` declarations bind to C functions
4. Wrapper functions handle marshalling via `ContT` for bracket-safe `alloca`/`with`

When adding a new C binding: define the `Storable*` type → add `GStorable` deriving → add `DomainMapping` instance → write `foreign import ccall` → write the Haskell wrapper.

### API Structure

Servant routes defined as types in `Routes/`, handlers in `Handlers/`. Game modes:
- **Hotseat**: Synchronous REST — client sends moves, server validates + returns state
- **AI**: REST + engine search — server makes AI moves via the Search effect
- **Online**: REST + WebSocket — real-time multiplayer with draw/undo negotiation

## Code Style

### Language & Extensions

GHC2021 base with these default extensions: `TemplateHaskell`, `OverloadedStrings`, `OverloadedRecordDot`, `OverloadedLabels`, `DuplicateRecordFields`, `LambdaCase`, `DerivingVia`, `ImportQualifiedPost`, `MultilineStrings`.

### Key Conventions

- **Prelude**: Relude (replaces base Prelude). Use `Text` everywhere, never `String`
- **Record access**: `record.field` syntax (OverloadedRecordDot). When importing a record type, always use `(..)` — e.g. `import Module (SomeRecord (..))` — or dot syntax won't work
- **Lenses**: optics library (`^.`, `%~`, etc.)
- **Warnings**: `-Wall -Werror -Wmissing-export-lists` — every module needs an explicit export list
- **Formatting**: fourmolu (2-space indent, leading commas, 80-column, trailing arrows, diff-friendly imports)

### Common Pitfalls

- Forgetting `(..)` on record imports → cryptic compile error about missing field accessors
- Using `String` instead of `Text` → type mismatch (Relude doesn't export `String` as default)
- Missing export list → `-Wmissing-export-lists` error
- Editing `hnefatafl.cabal` directly → changes overwritten by next `hpack` run
- Not running `cabal clean` after C library rebuild → stale object files linked

## Logging

Structured JSON logging via Katip (`Hnefatafl.Effect.Log`). Log at severity that reflects **operator concern**, not user outcome. A 400 to the user is not automatically an error; a 500 always is.

| Level | Meaning | Examples |
|---|---|---|
| `DebugS` | Developer diagnostics. Off in prod. Includes normal external client misbehavior we reject by design (unauth requests, garbage payloads from unknown clients). | Timings, parsed message dumps |
| `InfoS` | Lifecycle and state changes an operator scanning the log would want to see. Rare enough to not be noise. | Server start, player connect/disconnect, game created |
| `WarningS` | A contract inside **our own system** was violated but we handled it correctly. The "we handled it" is the handling; the "it happened at all" is the anomaly. Frontend is part of our system. | Frontend sent an illegal move (should have been filtered client-side), stale state, retries, fallback paths |
| `ErrorS` | Something broke that we couldn't fully handle. A request failed; the system itself is still healthy. | Caught unexpected exception → 500, invariant violation, engine crash during search |
| `CriticalS` | The system itself is at risk, not just one request. | DB unreachable after retries, server shutting down on fatal error, repeated background-task failures |

Key distinctions:
- **Warning vs Error** is "did we handle it?", not "is it bad?". A well-handled 400 can be a warning at most; a 500 is always at least an error.
- **User input errors are not warnings** *unless* the input came from a component we control (frontend). External-client garbage is Debug; frontend contract violations are Warning.
- **Logs ≠ metrics.** Things you'd naturally count (rate of invalid moves, rate of undos) belong in metrics. Until metrics infra exists, prefer lower log levels over flooding higher ones.

## Database

SQLite via `sqlite-simple`. Schema in `db/schema.sql`. Key tables:

| Table | Purpose |
|---|---|
| `game` | Base game record (type, status, timestamps) |
| `hotseat_game` / `ai_game` / `online_game` | Mode-specific game data |
| `move` | Move history (sequential, with board state snapshot per move) |
| `player` / `human_player` / `engine_player` | Player records |
| `game_participant_token` | Auth tokens for game access |
| `pending_game_action` | Draw offers, undo requests |

Migrations managed via dbmate. Schema version tracked in `schema_migrations`.

## Testing

Tasty framework with `tasty-discover` (auto-discovers `*Test.hs` files in `test/`). Includes HSpec, HUnit, and QuickCheck tests.

```
cabal test                          # run all tests
cabal test --test-option='-p "foo"' # filter by pattern
```

Test utilities in `test/TestUtil.hs` and `test/Hnefatafl/Game/TestUtil.hs`.

## Don't Change the User's Approach

When implementing solutions, follow the specified approach exactly. If there are issues, fix implementation details rather than changing the approach. Ask before deviating.
